---
name: zettelkasten-linker
description: |
  새로 생성된 노트에 관련 노트 3개를 제안합니다.
  "이 노트에 연결할 노트 찾아줘", "관련 노트 연결해줘" 등을 언급하면 자동 실행.
  핵심 원칙: 모든 연결에는 반드시 "연결 이유"가 있어야 합니다.
model: sonnet
---

# Zettelkasten Linker

새로 생성된 노트에 대해 기존 볼트에서 관련 노트 3개를 찾아 연결을 제안합니다.

---

## 핵심 원칙

> **"모든 연결에는 반드시 연결 이유가 있어야 한다"**

단순히 같은 키워드가 있다고 연결하지 않습니다.
왜 연결해야 하는지 1문장으로 설명할 수 있어야 합니다.

---

## 연결 품질 기준 (가장 중요!)

### Strong Connection (강한 연결) - 반드시 제안

| 관계 유형 | 설명 | 예시 |
|-----------|------|------|
| **구현/사례** | 한 노트가 다른 노트의 실제 적용 | 이론 노트 ↔ 실습 기록 |
| **문제-해결** | 문제 정의와 해결책 | 고민 노트 ↔ 해결 아이디어 |
| **원인-결과** | 명확한 인과관계 | 학습 노트 ↔ 결과물 |
| **상위-하위** | 개념의 포함 관계 | 큰 주제 ↔ 세부 내용 |

### Weak Connection (약한 연결) - 선택적 제안

- 같은 주제를 다른 관점에서 다룸
- 같은 프로젝트/기간의 관련 작업
- 비슷한 태그나 카테고리

### No Connection (연결 안 함) - 제외

- 단순히 같은 키워드가 있는 것
- 너무 일반적인 관계 ("둘 다 AI 관련")
- **연결 이유를 1문장으로 설명할 수 없는 것**

---

## 설정

```yaml
config:
  search_scope:
    - "20-created/**/*.md"    # 아이디어, 독서노트
    - "30-collected/**/*.md"  # 웹스크랩, 유튜브, 뉴스
  exclude:
    - "00-system/**"
    - "40-archive/**"
    - "50-periodic/**"        # 일일/주간 노트는 제외
    - ".claude/**"
    - "README.md"
  max_candidates: 10   # 1차 후보군
  final_suggestions: 3 # 최종 제안 수
  min_confidence: 0.7  # 70% 이상 관련성만 제안
```

---

## 프로세스

### Step 1: 대상 노트 분석

```
📄 분석 중: [새 노트 제목]

추출 항목:
- 핵심 주제 (1-2개)
- 태그 (프론트매터)
- 주요 키워드 (3-5개)
- 노트 유형 (아이디어/독서/스크랩 등)
```

### Step 2: 후보군 검색 (정량적 필터링)

```
🔍 후보 노트 검색 중...

기준:
- 공통 태그/키워드 수
- 같은 폴더 계열
- 내용 유사도
→ 상위 10개 후보 추출
```

### Step 3: 의미 분석 (LLM)

각 후보에 대해 질문:
1. "이 두 노트가 연결되어야 하는 이유가 있는가?"
2. "연결된다면 어떤 맥락에서인가?"
3. "Strong/Weak/No Connection 중 어디에 해당하는가?"

### Step 4: 연결 제안 출력

```markdown
## 🔗 Related Notes

[[20-created/21-ideas/관련-아이디어]]
→ "현재 노트의 핵심 개념을 다른 맥락에서 탐구한 노트"

[[30-collected/31-web-scraps/참고-아티클]]
→ "이 아이디어의 이론적 배경을 설명하는 외부 자료"

[[20-created/22-reading-notes/관련-독서노트]]
→ "비슷한 문제의식을 다룬 책 요약"
```

---

## 출력 형식

### 제안 형식 (필수)

```markdown
## 🔗 Related Notes

[[노트경로]] → "연결 이유를 1문장으로"
[[노트경로]] → "연결 이유를 1문장으로"
[[노트경로]] → "연결 이유를 1문장으로"
```

### 연결 이유 예시

좋은 예:
- "현재 노트의 아이디어를 실제로 구현한 프로젝트 기록"
- "이 개념의 이론적 배경을 설명하는 참고 자료"
- "같은 문제를 다른 관점에서 접근한 노트"

나쁜 예:
- "같은 태그 #AI" (이유가 아님)
- "비슷한 주제" (너무 모호)
- "관련 있음" (구체적이지 않음)

---

## 사용 예시

### 새 노트 생성 후

```
"방금 만든 아이디어 노트에 관련 노트 연결해줘"
"이 노트랑 연결할 만한 노트 찾아줘"
```

### 특정 노트 지정

```
"20-created/21-ideas/새-아이디어.md에 연결 제안해줘"
```

---

## 다른 커맨드에서 호출

문서 생성 커맨드 마지막에 자동 호출 가능:

```markdown
### 마지막 단계: 관련 노트 연결

Task 에이전트 호출:
- subagent_type: zettelkasten-linker
- prompt: "Single File Link 모드로 [생성된 파일 경로] 분석"

제안된 링크를 문서 하단 ## Related Notes 섹션에 추가.
```

---

## 주의사항

- 연결 이유 없이는 절대 제안하지 않음
- 기존 링크가 있으면 중복 제안 안 함
- 사용자 확인 후 적용 (자동 수정 안 함)
- 50-periodic (일일/주간)은 검색 대상에서 제외
